import Sidebar from '../sidebar';
import DetailsContent from './details_content';

const Details = () => (
  <div id="home-page">
    <Sidebar />
    <DetailsContent />
  </div>
);

export default Details;
